<%- include("header"); -%>;
    <div class="box" id="heading">
      <h1><%= listTitle %></h1>
    </div>
    <div class="box">
     <% NewListItems.forEach(function(item){ %>
      <form action="/delete" method="post">
        <div class="item">
          <input type="checkbox" name="cb" value="=<%= item._id %>" onChange="this.form.submit()" />
          <p><%= item.name %></p>
        </div>
        <input type="hidden" name="listName" value="<%= listTitle %>"></input>
      </form>
     <% }) %>
      <!-- <% for(let it of NewListItems){ %>
      <div class="item">
        <input type="checkbox" />
        <p><%= it.name %></p>
      </div>
      <% } %> -->

      <!-- post method will post the information from form to the server -->
      <form class="item" action="/today" method="post">
        <input type="text" name="NewItem"  placeholder="New Item">
        <button type="submit" name="list"  value=<%= listTitle %> >+</button>
      </form>
    </div>
    <%- include("footer"); -%>;